import { FilterWrapper, NavItem } from "./styles";

const Filter = () => {
  return (
    <FilterWrapper>
      <NavItem>New Arrivals</NavItem>
      <NavItem>Best Sellers</NavItem>
      <NavItem>Sale Items</NavItem>
    </FilterWrapper>
  );
};

export default Filter;
